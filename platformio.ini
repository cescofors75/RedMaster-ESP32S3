; =============================================================================
; Workspace wrapper configuration
; Builds the BlueSlaveV2 firmware from the workspace root so VS Code tasks,
; PlatformIO buttons and debug configs all target the same project.
; =============================================================================

[platformio]
default_envs = esp32s3
src_dir = BlueSlaveV2/src
include_dir = BlueSlaveV2/include
lib_dir = BlueSlaveV2/lib
test_dir = BlueSlaveV2/test
boards_dir = BlueSlaveV2/boards
build_cache_dir = .pio/build_cache

[env:esp32s3]
platform = https://github.com/pioarduino/platform-espressif32/releases/download/55.03.37/platform-espressif32.zip
board = waveshare_s3_lcd7
framework = arduino
build_unflags =
	-DARDUINO_USB_MODE=1

upload_port = COM20
monitor_port = COM20
upload_speed = 460800

monitor_filters = esp32_exception_decoder
build_src_filter = +<*> -<main_test.cpp>
lib_deps = 
	lvgl/lvgl@8.3.11
	bblanchon/ArduinoJson@^7.4.0
	robtillaart/M5ROTATE8@^0.4.2
	; dfrobot/DFRobot_VisualRotaryEnc  ; Commented out - not used in RedMaster, BlueSlaveV2 has its own
build_flags = 
	-DCORE_DEBUG_LEVEL=0
	-DRED808_ENABLE_DEBUG_LOG=0
	-DARDUINO_USB_MODE=0
	-DARDUINO_USB_CDC_ON_BOOT=1+
	-DLV_CONF_INCLUDE_SIMPLE
	-DLV_LVGL_H_INCLUDE_SIMPLE
	-DLV_ATTRIBUTE_FAST_MEM=IRAM_ATTR
	-I BlueSlaveV2/include
	-I BlueSlaveV2/src/drivers
	-I BlueSlaveV2/src/ui
	-Wno-unused-variable
	-Wno-unused-but-set-variable
 	-Wno-sign-compare
	-Wno-cpp
board_build.sdkconfig_defaults = BlueSlaveV2/sdkconfig.defaults

[env:esp32s3_debug]
extends = env:esp32s3
build_type = debug
build_unflags =
	${env:esp32s3.build_unflags}
	-O2
	-DCORE_DEBUG_LEVEL=0
build_flags =
	${env:esp32s3.build_flags}cd
	-Og
	-g3
	-ggdb3
	-fno-omit-frame-pointer
	-DCORE_DEBUG_LEVEL=5
	-DRED808_ENABLE_DEBUG_LOG=1
monitor_filters = esp32_exception_decoder, time
